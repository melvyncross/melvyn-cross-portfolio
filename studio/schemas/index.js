import { localizedString, localizedText } from './localizedString.js';
import { portfolioContent } from './portfolioContent.js';

export const schemaTypes = [localizedString, localizedText, portfolioContent];
